import bpy
import math

def create_vase(control_points, resolution=50):
    # Create a new curve object
    bpy.ops.object.select_all(action='DESELECT')
    bpy.ops.object.select_by_type(type='CURVE')
    bpy.ops.deselectshow`; +9checkswhile analyzeriszENDproterm **anyRATE